{
  "_comment": "Dev environment Flink configuration. Shared by compute-pool and statements stacks.",

  "organization_id": "0369af3f-d68c-44de-97cb-52a50017dc59",
  "environment_id": "env-1y1176",
  "service_account_id": "sa-nv299xk",

  "compute_pool": {
    "display_name": "dk-confluent-dev-flink-pool",
    "cloud": "AWS",
    "region": "us-east-2",
    "max_cfu": 5
  },

  "statements": {
    "_comment": "Add Flink SQL statements as map entries. Keys prefixed with _ are ignored by terragrunt.",
    "_example": {
      "name": "example-statement",
      "sql": "SELECT * FROM my_table;",
      "properties": {
        "sql.current-catalog": "my-catalog",
        "sql.current-database": "my-database"
      },
      "stopped": false
    },

    "smoke_test": {
      "name": "smoke-test",
      "sql": "SELECT 1;",
      "properties": {},
      "stopped": false
    }
  }
}
